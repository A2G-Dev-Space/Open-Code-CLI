/**
 * LLM Agent Tools Index
 *
 * LLM이 tool_call로 호출하는 도구들 (Sub-LLM 사용)
 *
 * TODO: Add agent tools like:
 * - Code analysis tool (uses Sub-LLM for deep analysis)
 * - Test generation tool (uses Sub-LLM for intelligent test creation)
 * - Refactoring suggestion tool (uses Sub-LLM for pattern detection)
 */

import { LLMAgentTool } from '../../types.js';

// Placeholder for future LLM agent tool implementations
export const LLM_AGENT_TOOLS: LLMAgentTool[] = [];
